{
  "type": "module",
  "dependencies": {
    "@nanostores/persistent": "^1.1.0",
    "nanostores": "^1.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  },
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch"
  }
}